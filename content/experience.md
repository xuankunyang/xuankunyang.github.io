---
title: 'Experience'
# Education and Experience moved onto the homepage; this page is only here so
# the old URL keeps working. Hugo will not emit an `aliases` entry that points
# at the home page, and an alias could not carry the `#experience` fragment
# anyway, so it redirects explicitly instead.
layout: redirect
redirect_to: '/#experience'
sitemap:
  disable: true
_build:
  list: never
---
